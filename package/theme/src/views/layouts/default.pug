include ../variables
include ../mixins

<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        block title
            title=page.title
    body
        block top
            include ../components/top.pug
        block header
            include ../components/header
        block main
            h1=(page.title + ' page').toUpperCase()
            p content Layouts
            +button('Đăng ký')
            p Default content
        block footer
            include ../components/footer