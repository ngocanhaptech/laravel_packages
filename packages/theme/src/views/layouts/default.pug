include ../variables
include ../mixins
<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        block title
            title=page.title
        block styles
            link(rel="stylesheet", href="/assets/bootstrap-4.4.1/css/bootstrap.min.css")
    body
        block top
            include ../commons/top
        block header
            include ../commons/header
        block main
            h1=(page.title + ' page').toUpperCase()
            p content Layouts
            +button('Đăng ký')
            p Default content
        block footer
            include ../commons/footer
        block scripts
            script(src="/assets/jquery/jquery-3.4.1.slim.min.js")
            script(src="/assets/bootstrap-4.4.1/js/bootstrap.min.js")