mixin switch(_switch_id, data = { className: 'md' })
    - const switch_id = _switch_id || 'switch_input'
    .switch(class = data.className )
        input(type="checkbox", id=switch_id)
        label(for=switch_id)
            span.label-off HDD
            span.label-on SSD